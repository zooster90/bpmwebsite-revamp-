*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Local Netlify folder
.netlify

# Garbage file from shell error
count())

# Static export (regenerated by build, also too large for git)
/dist

# Large user-uploaded media (host on object storage / Laravel Cloud disk, not git)
# Keep small project/award images; exclude only the giant gallery (~1.8 GB each)
/public/images/records
/public/img/images/records
/public/storage

# Local SQLite db if developer creates one
/database/database.sqlite

# IDE
/.windsurf
/.trae
/.augment

# Local scratch / data dumps from intern handoffs (not for repo)
/newcodefile

